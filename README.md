# Transformation-Task-
This project builds a complete ETL pipeline using SSIS to process client-provided CSV files. The data is extracted, cleaned, validated, and transformed using SSIS transformation tasks. Finally, the transformed and quality-checked data is loaded into SQL Server.
